import "./Select.css";

const Select = ({ title }) => {
    return (
        <select className="custom-select">
            <option>So many options</option>
            <option>...</option>
        </select>
    );
};

export default Select;